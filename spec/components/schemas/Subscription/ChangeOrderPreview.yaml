type: object
properties:
  difference:
    type: object
    readOnly: true
    properties:
      removed:
        type: object
        properties:
          items:
            type: array
            items:
              $ref: "#/components/schemas/InvoiceItem"
          issuedTime:
            description: Null, if issuedTime wasn't changed.
            nullable: true
            allOf:
                - $ref: "#/components/schemas/ServerTimestamp"
          dueTime:
            description: Null, if dueTime wasn't changed.
            nullable: true
            allOf:
              - $ref: "#/components/schemas/ServerTimestamp"
      added:
        type: object
        properties:
          items:
            type: array
            items:
              $ref: "#/components/schemas/InvoiceItem"
          issuedTime:
            description: Null, if issuedTime wasn't changed.
            nullable: true
            allOf:
              - $ref: "#/components/schemas/ServerTimestamp"
          dueTime:
            description: Null, if dueTime wasn't changed.
            nullable: true
            allOf:
              - $ref: "#/components/schemas/ServerTimestamp"
  deferred:
    description: Deferred Invoice preview
    readOnly: true
    allOf:
      - $ref: "#/components/schemas/Invoice"
  interim:
    description: Interim Invoice preview
    readOnly: true
    allOf:
      - $ref: "#/components/schemas/Invoice"
  upcoming:
    description: Upcoming Invoice preview
    readOnly: true
    allOf:
      - $ref: "#/components/schemas/Invoice"
