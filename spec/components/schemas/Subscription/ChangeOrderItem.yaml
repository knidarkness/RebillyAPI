type: object
discriminator:
  propertyName: changeType
  mapping:
    "item-remove": "#/components/schemas/ChangeOrderItemRemove"
    "plan-add": "#/components/schemas/ChangeOrderPlanAdd"
    "renewal-time": "#/components/schemas/ChangeOrderRenewalTime"
    "billing-anchor": "#/components/schemas/ChangeOrderBillingAnchor"
    #"pricing": "#/components/schemas/ChangeOrderPricing"
properties:
  id:
    description: Change Order Item ID.
    allOf:
      - $ref: "#/components/schemas/ResourceId"
  changeType:
    description: Change Type.
    type: string
    enum:
      - "item-remove"
      - "plan-add"
      - "renewal-time"
      - "billing-anchor"
      #- "pricing"
  effectiveTime:
    description: Change effective time. Set to null for next renewal time.
    type: string
    format: date-time
    nullable: true
  proration:
    description: Defines if this change will be prorated.
    type: boolean
  createdTime:
    description: The change created time
    allOf:
      - $ref: "#/components/schemas/ServerTimestamp"
